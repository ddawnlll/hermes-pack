# Profile: worker-N — bounded executor. Cheap, fast, no spawning.
provider: __HERMES_WORKER_PROVIDER__
model: __HERMES_WORKER_MODEL__

delegation:
  orchestrator_enabled: false

goals:
  max_turns: 15
